
import React from 'react';
import styles from './InfoTitle.module.scss';

interface InfoTitleProps { }

// eslint-disable-next-line no-empty-pattern
export const InfoTitle = ({ }: InfoTitleProps) => (
	<div className={styles.infoTitle}>
    InfoTitle Component
	</div>
);
